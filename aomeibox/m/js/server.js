var nav=document.getElementById('nav'),	popNav=document.getElementById('popNav');nav.addEventListener('click',function(even){	if(hasClassName(nav,'active')){		removeClassName(nav,'active');		removeClassName(popNav,'active');	}else{		addClassName(nav,'active');		addClassName(popNav,'active');	}});function hasClassName(el,clsName){	return (new RegExp('(\\s|^)' + clsName + '(\\s|$)')).test(el.className)}function addClassName(el,clsName){	if(!hasClassName(el,clsName)){		el.className+=' ' + clsName;	}}function removeClassName(el,clsName){	if(hasClassName(el,clsName)){		var reg=new RegExp('(\\s|^)' + clsName +'(\\s|$)');		el.className=el.className.replace(reg,'');	}}